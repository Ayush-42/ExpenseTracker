# Port 3000 is a common default and often taken by another dev server,
# so the API uses 4000. Keep VITE_API_URL in client/.env pointing at this port.
PORT=4000
CLIENT_URL=http://localhost:5173
MONGODB_URI=mongodb://localhost:27017/expense-tracker
